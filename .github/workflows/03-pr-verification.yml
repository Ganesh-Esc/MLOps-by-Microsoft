name: Code Verification on Pull Request

# Trigger this workflow only when a Pull Request is opened, reopened, or updated
on:
  pull_request:
    branches:
      - main # Only run PRs targeting the main branch

jobs:
  verify_code:
    runs-on: ubuntu-latest
    steps:
      # Standard step to check out the code
      - name: Check out repo
        uses: actions/checkout@main

      # The placeholder step required by the instructions
      - name: Placeholder
        run: |
          echo "Will add code checks here in next challenge"
